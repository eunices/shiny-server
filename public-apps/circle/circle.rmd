---
runtime: shiny
title: "Monte carlo simulation of circle to find value of pi"
output: 
  html_document:
    toc: true
    theme: flatly
---

```{r}
knitr::opts_chunk$set(echo=TRUE, message=TRUE, warning=TRUE)
```


```{r}
actionButton("counter", "Add")
```

```{r}
randomVals <- eventReactive(input$counter, {
    runif(100)
})

renderPlot({
    hist(randomVals())
})
```